#!/usr/bin/env python3
"""ftree - Family tree viewer executable."""

import sys
import os

# Add parent directory to path so we can import src modules
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from src.cli import main

if __name__ == '__main__':
    sys.exit(main())